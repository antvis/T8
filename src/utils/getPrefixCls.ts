/**
 * Gets the prefix class name for a given suffix class name.
 * @param suffixCls - The suffix class name to append to the prefix.
 */
export const getPrefixCls = (suffixCls?: string) => `ntv-${suffixCls}`;
