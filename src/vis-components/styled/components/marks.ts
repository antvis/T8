import { createStyledComponent } from '../styledFactory';

export const Bold = createStyledComponent({
  element: 'strong',
});

export const Italic = createStyledComponent({
  element: 'em',
});

export const Underline = createStyledComponent({
  element: 'u',
});
