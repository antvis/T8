/**
 * create single line
 * 用于创建单行编辑，应用场景是只做短语片段模版编辑
 * https://plate.udecode.io/docs/plugins/single-line
 */
import { createSingleLinePlugin } from '@udecode/plate-break';

export const singleLinePlugin = createSingleLinePlugin();
